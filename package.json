{
  "name": "discoteca-online",
  "scripts": {
    "dev": "npm --prefix frontend run dev",
    "dev:frontend": "npm --prefix frontend run dev",
    "dev:backend": "python -m uvicorn main:app --reload --port 5000 --app-dir backend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
